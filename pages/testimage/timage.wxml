<cu-custom bgColor="bg-orange" isBack="{{true}}">
<view slot="backText">返回</view>
<view slot="content"></view>
</cu-custom>

<!--  -->
<xing-editor bindfinish="finish" output-type="array" image-upload-url="{{serverUrl}}" image-upload-name="image" image-upload-key-chain="{{serverUrl}}" html="{{html}}" />
<!--  -->
<!-- style="width:100%;height:80px;border-top:1px solid red;background-color:#f0f0f0;position:fixed; bottom:0;"> -->
<!-- <view class="cu-bar bg-white tabbar border shop" style="width:100%;height:60px;border-top:1px solid red;background-color:#f0f0f0;position:fixed; bottom:0;">
    <button class="action" open-type="contact">
      <view class="cuIcon-service text-green">
        <view class="cu-tag"></view>
      </view> 客服
    </button>
    <view class="action"  bindtap='getmyproject'>
      <view class="cuIcon-tag">
        <view class="cu-tag badge">{{count}}</view>
      </view>
      已有项目
    </view>
    <view class="btn-group">
      <button class="cu-btn bg-orange round shadow-blur" bindtap='uploadfile'>
      <view hidden='{{uploadfile.size==0?true:flase}}' class="cuIcon-tag">
        <view class="cu-tag badge">{{uploadfile.size}}</view>
      </view>
      上传文件
      </button>
      <button class="cu-btn bg-red round shadow-blur" bindtap='pushProject'>发布项目</button>
    </view>
</view> -->